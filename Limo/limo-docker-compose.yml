version: '3.8'

services:
  limo-node-server: 
    restart: always
    build: 
      context: "./node-server"
    environment:
      - BACKEND_IP

  limo-ros-server:
    restart: always
    build: 
      context: "./ros-server"
    depends_on:
      # - limo-bridge-server
      - limo-node-server
    environment:
      - LIMO_IP


  # limo-bridge-server:
  #   restart: always
  #   build: 
  #     context: "./bridge-server"

