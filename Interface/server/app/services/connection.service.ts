import { TYPES } from '@app/types';
import { inject, injectable } from 'inversify';
@injectable()
export class ConnectionService {
    // constructor(@inject(TYPES.*Your services here*) private *service*: *ServiceName*) {

    // }
}
